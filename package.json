{
  "name": "exMate",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm install --prefix frontend\" \"npm run dev --prefix frontend\" \"cd backend && ./mvnw spring-boot:run\"",
    "install-deps": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "@headlessui/react": "^2.2.0",
    "axios": "^1.7.7",
    "react-router-dom": "^6.27.0"
  }
}
